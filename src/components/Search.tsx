import React, {FC} from 'react';

interface SearchProps {
    isShow: boolean
}

const Search: FC<SearchProps> = ({isShow}) => {
    return (
        <div style={{}} className={'search-wrapper'}>
            
        </div>
    );
};

export default Search;